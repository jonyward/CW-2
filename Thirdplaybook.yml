- name: CW2 Playbook
  hosts: localhost
  user: ubuntu

  tasks:

  - name: Pull Docker image
    shell:
      cmd: docker pull jonyward/final-app

  - name: Deploy to kubernetes
    shell:
      cmd: kubectl create deployment kubernetes-server --image=jonyward/final-app
